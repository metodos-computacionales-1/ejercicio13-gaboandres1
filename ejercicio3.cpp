#include <iostream>
long fibonacci(int n)
{
    if (a>1)
        return (a*factorial(a-1));
    else
        return 1;
}

int main(void)
{
    int number=9;
    z=addition(x,y);
    std::cout<< number<<"! = " <<factorial(number)<<"\n";
    return 0;
}